syntax = "proto3";
package simulation;

message NetPacket
{
    enum PacketType
    {
        CLIENT_INPUT = 0;
        SERVER_STATE = 1;
    }

    PacketType type = 1;

    uint32 sender_id = 2;

    string payload = 3;

}